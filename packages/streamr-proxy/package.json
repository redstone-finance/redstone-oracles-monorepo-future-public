{
  "name": "@redstone-finance/streamr-proxy",
  "prettier": "redstone-prettier-config",
  "version": "0.3.6",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "NODE_ENV=test yarn global:jest",
    "build": "rm -rf dist && yarn global:tsc --build tsconfig.build.json",
    "lint": "yarn global:eslint .",
    "preversion": "yarn build"
  },
  "dependencies": {
    "pako": "^2.1.0",
    "streamr-client": "^7.2.1"
  },
  "devDependencies": {
    "@types/pako": "^2.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
